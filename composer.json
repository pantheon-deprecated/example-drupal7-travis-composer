{
    "name": "pantheon-systems/example-drupal7-composer",
    "description": "Run `composer install` followed by `bin/init-new-project` to replace this file and set up your starting project.",
    "require": {
    },
    "require-dev": {
        "pantheon-systems/travis-scripts": "1.*"
    },
    "scripts": {
        "post-create-project-cmd": [
            "./bin/init-new-project"
        ]
    },
    "config": {
        "bin-dir": "bin/"
    }
}
